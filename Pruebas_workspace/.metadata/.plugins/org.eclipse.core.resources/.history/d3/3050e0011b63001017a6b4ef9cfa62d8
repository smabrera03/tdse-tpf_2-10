En este proyecto voy a intentar hacer andar el LCD con I2C

La dirección del módulo es 0x27.
NOTA IMPORTANTE: Con solo crear la carpeta con los .h y .c no alcanza, hay que indicarle al IDE que esa carpeta
contiene archivos a linkear de la siguiente manera:
click derecho en el nombre del proyecto>>>Properties>>>C/C++ General>>>Paths and symbols>>>ir a la solapa de 
source locations>>>agergar la carpeta con los archivos .c y .h

Del repo del tutorial descargué:

>>>Util.h y .c
Algunas macros y cosas que no entiendo
Lo interesante es DELAY_US, que hace el delay en us con una macro, NO con una función
La diferencia es que cada vez que llames a DELAY_US(us) en tu código, el compilador va a reemplazar esa linea por


do { \
        for (uint32_t i = 0; i < ms; ++i) { \
            DELAY_US(1000); \
        } \
    } while (0)
    
    
que es lo que está definido en Util.h. La diferencia entre una macro y una función es este reemplazo de código
y que la macro no tiene retorno, no devuelve nada.


>>>I2C_LCD_cfg.h y .c


>>>I2C_LCD.h y .c

También configuré los pines de I2C:

PB10>>SCL (D6)
PB11>>SDA (El que está en frente a D10 en el CN10)